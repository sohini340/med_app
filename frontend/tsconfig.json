{
  "compilerOptions": {
    "baseUrl": ".",

    "paths": {
      "@/*": ["src/*"]
    },

    "allowJs": true,
    "skipLibCheck": true,

    /* keep it relaxed for now */
    "strict": false
  },

  "files": [],

  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}